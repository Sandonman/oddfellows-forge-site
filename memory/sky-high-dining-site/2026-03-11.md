# sky-high-dining-site — 2026-03-11

## Goal
- Add automatic receipt email after successful payment.

## Last Completed
- Updated `Sky_High_Dining_Site/src/api/server.js` Stripe webhook handler.
- On `checkout.session.completed`, API now:
  - updates reservation payment status to `paid`
  - fetches reservation/customer details in the same query
  - sends a receipt email to the customer with paid amount, reservation ID, reservation time, and location.
- Ran `node --check src/api/server.js` (passes).

## Next Step
- Deploy/restart API and run a Stripe test payment to confirm receipt email delivery end-to-end.

## Notes
- Receipt email sending uses existing `sendEmail()` helper + `RESEND_API_KEY`.
- If webhook event replays, this path currently sends receipt again; if needed, add a guard to send only on first transition to paid.
